#include "terminator.def"

DO_HANDLE(INSTRUCTION_ADD, add)
DO_HANDLE(INSTRUCTION_SUB, sub)
DO_HANDLE(INSTRUCTION_ALLOCA, alloca)
DO_HANDLE(INSTRUCTION_LOAD, load)
DO_HANDLE(INSTRUCTION_STORE, store)
DO_HANDLE(INSTRUCTION_ICMP_NE, icmp_ne)
