#include "handle.def"

DO_HANDLE(NIL, nil)
DO_HANDLE(LIST, list)
DO_HANDLE(ARRAY, array)
DO_HANDLE(CALL, call)
DO_HANDLE(OLD, old)
DO_HANDLE(ID, id)
DO_HANDLE(BINARY, binary)
DO_HANDLE(ARITY1, arity1)

#include "handle-end.def"
